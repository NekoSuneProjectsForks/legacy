name: Build & Publish to Docker Hub
on:
  workflow_call:
    inputs:
      base_version:
        description: 'Base version (latest or develop)'
        required: true
        type: string
      info_version:
        description: 'Informational version number (e.g. 0.3.7-alpha)'
        required: true
        type: string
      tag_version:
        description: 'Docker tag version (e.g. v0.3.7)'
        required: true
        type: string
    secrets:
      docker_hub_username:
        required: false
      docker_hub_access_token:
        required: false
jobs:
  build_images:
    name: Build ${{ matrix.name }} image
    runs-on: ${{ matrix.os }}
    if: contains(github.event.head_commit.message, '[no build]') == false
    strategy:
      matrix:
        include:
          - name: amd64
            os: ubuntu-latest
            path: ''
            suffix: '-amd64'
            platform: 'linux/amd64'
          - name: arm32v7
            os: ubuntu-latest
            path: 'arm32v7/'
            suffix: '-arm'
            platform: 'linux/arm/v7'
          - name: arm64
            os: ubuntu-24.04-arm
            path: 'arm64/'
            suffix: '-arm64'
            platform: 'linux/arm64'
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Download next artifacts
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          case "${{ matrix.name }}" in
            amd64) TARGET="linux-x64" ;;
            arm32v7) TARGET="linux-arm" ;;
            arm64) TARGET="linux-arm64" ;;
          esac
          gh release download develop --repo ErsatzTV/next --pattern "ersatztv-next-*-${TARGET}.tar.gz" --dir next-download
          mkdir next-extracted
          tar xzvf next-download/*.tar.gz -C next-extracted --strip-components 1
          mv next-extracted/ersatztv-channel .
          rm -rf next-download next-extracted

      - name: Set up QEMU
        if: ${{ matrix.name == 'arm32v7' }}
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to the Container registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push by digest
        id: build
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./docker/${{ matrix.path }}Dockerfile
          push: true
          provenance: false
          platforms: ${{ matrix.platform }}
          build-args: |
            INFO_VERSION=${{ inputs.info_version }}-docker${{ matrix.suffix }}
          outputs: |
            type=image,name=ghcr.io/nekosuneprojectsforks/legacy,name-canonical=true,push-by-digest=true

      - name: Save digest to artifact
        run: echo ${{ steps.build.outputs.digest }} > digest.txt

      - name: Upload digest artifact
        uses: actions/upload-artifact@v6
        with:
          name: digest-${{ matrix.name }}
          path: digest.txt

  merge_manifests:
    name: Merge Manifests
    runs-on: ubuntu-latest
    needs: build_images
    steps:
      - name: Log in to the Container registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Download all digest artifacts
        uses: actions/download-artifact@v6
        with:
          path: digests/

      - name: Read digests from artifacts
        id: digests
        run: |
          AMD64_HASH=$(cat digests/digest-amd64/digest.txt)
          ARM32V7_HASH=$(cat digests/digest-arm32v7/digest.txt)
          ARM64_HASH=$(cat digests/digest-arm64/digest.txt)

          GHCR_DIGESTS="ghcr.io/nekosuneprojectsforks/legacy@${AMD64_HASH} ghcr.io/nekosuneprojectsforks/legacy@${ARM64_HASH} ghcr.io/nekosuneprojectsforks/legacy@${ARM32V7_HASH}"

          echo "ghcr_digests=${GHCR_DIGESTS}" >> $GITHUB_OUTPUT

      - name: Create and push manifests
        run: |
          docker manifest create ghcr.io/nekosuneprojectsforks/legacy:${{ inputs.base_version }} ${{ steps.digests.outputs.ghcr_digests }}
          docker manifest push ghcr.io/nekosuneprojectsforks/legacy:${{ inputs.base_version }}
          docker manifest create ghcr.io/nekosuneprojectsforks/legacy:${{ inputs.tag_version }} ${{ steps.digests.outputs.ghcr_digests }}
          docker manifest push ghcr.io/nekosuneprojectsforks/legacy:${{ inputs.tag_version }}
